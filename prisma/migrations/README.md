# Prisma Migrations

This directory is reserved for Prisma migration files generated by `prisma migrate dev`.

To create the first migration, run:

```bash
npm run prisma:migrate
```

If you need to reset the database during development, use:

```bash
npx prisma migrate reset
```
